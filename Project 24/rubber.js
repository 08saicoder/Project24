class Rubber{
    constructor(x, y, width, height){
        var options = {
            restitution:0.3,
            friction:5,
            density:1
        }
    }

    ball = Bodies.circle(200, 100, 20, ball_options);
  World;add(world, ball);
}